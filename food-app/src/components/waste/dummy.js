

            {/* <Tooltip title="Add Restaurant">
              <Button sx={{ backgroundColor: 'transparent', boxShadow: 'none', fontWeight: 'bold' }} variant="contained" onClick={handleOpenUserMenu}>Add Restaurant</Button>
            </Tooltip> */}

            {/* {!isLoggedIn && (
              <Tooltip title="Add Restaurant">
                <Button sx={{ backgroundColor: 'transparent', boxShadow: 'none', fontWeight: 'bold' }} variant="contained" onClick={handleOpenUserMenu}>Add Restaurant</Button>
              </Tooltip>
            )} */}

            {/* Only show "Add Restaurant", "Sign Up", and "Login" if admin is NOT logged in */}
            {/* {!isAdmin && (
              <>
                {!isLoggedIn && (
                  <Tooltip title="Add Restaurant">
                    <Button sx={{ backgroundColor: 'transparent', boxShadow: 'none', fontWeight: 'bold' }} variant="contained" onClick={handleOpenUserMenu}>Add Restaurant</Button>
                  </Tooltip>
                )} */}


                204
             {/* <Button sx={{ backgroundColor: 'transparent', boxShadow: 'none', fontWeight: 'bold' }} variant="contained" component={Link} to="/SignUp">SignUp</Button>
            <Button sx={{ backgroundColor: 'transparent', boxShadow: 'none', fontWeight: 'bold' }} variant="contained" component={Link} to="/Login">Login</Button> */}
